<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update File</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon/fonts/remixicon.css" rel="stylesheet">
</head>
<body>
    <div class="container mx-auto px-4 py-8 flex justify-center">
        <div class="w-full">
            <nav class="mb-8 border-b border-gray-200">
                <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                    <div class="flex items-center justify-between h-16">
                        <div class="flex-shrink-0">
                            <span class="text-3xl font-bold">Notes App</span>
                        </div>
                        <div class="hidden md:block">
                            <div class="flex items-baseline space-x-4">
                                <a href="/" class="px-3 py-2 rounded-md text-xl font-medium hover:bg-gray-100 hover:text-gray-900 transition duration-300">Home</a>
                            </div>
                        </div>
                    </div>
                </div>
            </nav>
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <form action="/update/<%= file._id %>" method="POST" autocomplete="off" class="space-y-6">
                    <div>
                        <label for="fileName" class="block text-sm font-medium text-gray-700">File Name</label>
                        <div class="mt-1">
                            <input type="text" name="fileName" id="fileName" value="<%= file.fileName %>" required class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm">
                        </div>
                    </div>
                    <div>
                        <label for="description" class="block text-sm font-medium text-gray-700">File Description</label>
                        <div class="mt-1">
                            <input type="text" name="description" id="description" value="<%= file.description %>" required class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm">
                        </div>
                    </div>
                    <div>
                        <label for="data" class="block text-sm font-medium text-gray-700">Data</label>
                        <div class="mt-1">
                            <textarea name="data" id="data" rows="3" required class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"><%= file.data %></textarea>
                        </div>
                    </div>
                    <div>
                        <button type="submit" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                            <i class="ri-save-line mr-2"></i> Update Note
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>